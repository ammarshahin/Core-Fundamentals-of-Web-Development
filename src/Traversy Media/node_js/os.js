const os = require(`os`);

// platform
console.log(os.platform());

// CPU Arch
console.log(os.arch());

// CPU cores
console.log(os.cpus());

// Free memory
console.log(os.freemem());

// total memory
console.log(os.totalmem());

//  Up time
console.log(os.uptime());
